<Window x:Class="CyberIncidentWPF.Views.AnalystNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analyst Name Required"
        Height="280" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F9FAFB">

    <Window.Resources>

        <!-- Border that looks like a modern textbox container -->
        <Style x:Key="ModernInputBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <!-- Focus state -->
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#4F46E5"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>

                <!-- Hover state -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#9CA3AF"/>
                </Trigger>

                <!-- Disabled state -->
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="👤 Enter Analyst Name"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Please enter your name to record who is changing this incident's status."
                   FontSize="13"
                   Foreground="#6B7280"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <!-- GUARANTEED WORKING INPUT (no ControlTemplate) -->
        <Border Grid.Row="2"
                Style="{StaticResource ModernInputBorderStyle}"
                Height="42"
                Margin="0,0,0,20"
                VerticalAlignment="Top">

            <TextBox x:Name="AnalystNameTextBox"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="12,9"
                     FontSize="14"
                     Foreground="#111827"
                     CaretBrush="#4F46E5"
                     VerticalContentAlignment="Center"
                     HorizontalContentAlignment="Left"
                     TabIndex="0"/>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Click="CancelButton_Click"
                    Background="#E5E7EB"
                    Foreground="#374151"
                    FontWeight="SemiBold"
                    Padding="24,10"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    TabIndex="2">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="OkButton"
                    Content="Confirm"
                    Click="OkButton_Click"
                    Background="#6366F1"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Padding="24,10"
                    Cursor="Hand"
                    TabIndex="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4338CA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Grid>
</Window>
