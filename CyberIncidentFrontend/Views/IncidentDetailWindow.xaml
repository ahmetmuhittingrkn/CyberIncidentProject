<Window x:Class="CyberIncidentWPF.Views.IncidentDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Incident Details" Height="600" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Incident Details" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Details -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Incident ID -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Incident ID:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding IncidentId}" Margin="0,0,0,15"/>

                    <!-- Title -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Title:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Title}" TextWrapping="Wrap" Margin="0,0,0,15"/>

                    <!-- Description -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" FontWeight="Bold" 
                               Margin="0,0,0,15" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Description}" 
                               TextWrapping="Wrap" Margin="0,0,0,15"/>

                    <!-- Type -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Type:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding IncidentType}" Margin="0,0,0,15"/>

                    <!-- Severity -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Severity:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SeverityLevel}" 
                               FontWeight="Bold" Margin="0,0,0,15">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SeverityLevel}" Value="CRITICAL">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SeverityLevel}" Value="HIGH">
                                        <Setter Property="Foreground" Value="Orange"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SeverityLevel}" Value="MEDIUM">
                                        <Setter Property="Foreground" Value="#FFA500"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SeverityLevel}" Value="LOW">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Status -->
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Status:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Status}" 
                               FontWeight="SemiBold" Margin="0,0,0,15">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="OPEN">
                                        <Setter Property="Foreground" Value="Blue"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="IN_PROGRESS">
                                        <Setter Property="Foreground" Value="Orange"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="RESOLVED">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="CLOSED">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Incident Date -->
                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Incident Date:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" 
                               Text="{Binding IncidentDate, StringFormat='yyyy-MM-dd HH:mm'}" Margin="0,0,0,15"/>

                    <!-- Reporter -->
                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Reporter:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding ReporterName}" Margin="0,0,0,15"/>

                    <!-- Created At -->
                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Created At:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="8" Grid.Column="1" 
                               Text="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" Margin="0,0,0,15"/>

                    <!-- Updated At -->
                    <TextBlock Grid.Row="9" Grid.Column="0" Text="Updated At:" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Grid.Row="9" Grid.Column="1" 
                               Text="{Binding UpdatedAt, StringFormat='yyyy-MM-dd HH:mm'}" Margin="0,0,0,15"/>

                    <!-- Resolved At -->
                    <TextBlock Grid.Row="10" Grid.Column="0" Text="Resolved At:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="10" Grid.Column="1" 
                               Text="{Binding ResolvedAt, StringFormat='yyyy-MM-dd HH:mm', TargetNullValue='Not resolved yet'}"/>
                </Grid>
            </Border>
        </ScrollViewer>

        <!-- Close Button -->
        <Button Grid.Row="2" Content="Close" Click="CloseButton_Click" 
                Background="#6C757D" Foreground="White" 
                Padding="30,10" Margin="0,15,0,0" 
                HorizontalAlignment="Center" FontSize="14"/>
    </Grid>
</Window>

