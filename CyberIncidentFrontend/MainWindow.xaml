<Window x:Class="CyberIncidentWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CyberIncidentWPF.Views"
        mc:Ignorable="d"
        Title="Cyber Incident Reporting &amp; Analysis Platform" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Window.Resources>
        <!-- Value Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Null to Bool Converter -->
        <Style x:Key="NullToBoolConverter" TargetType="Button">
            <Setter Property="IsEnabled" Value="False"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Inverse Bool Converter Style -->
        <Style x:Key="InverseBoolConverter" TargetType="Button">
            <Setter Property="IsEnabled" Value="True"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="True">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Button Hover Style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <Border Grid.Column="0" Background="#2C3E50">
            <StackPanel>
                <!-- Logo/Title -->
                <Border Background="#1A252F" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="🛡️ Cyber Security" FontSize="18" FontWeight="Bold" 
                                   Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Incident Platform" FontSize="12" 
                                   Foreground="#BDC3C7" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation Buttons -->
                <Button Content="📊 Incident List" 
                        Command="{Binding ShowIncidentListCommand}"
                        Style="{StaticResource NavButtonStyle}"/>
                
                <Button Content="➕ Create Incident" 
                        Command="{Binding ShowCreateIncidentCommand}"
                        Style="{StaticResource NavButtonStyle}"/>
                
                <Button Content="📈 Analytics Dashboard" 
                        Command="{Binding ShowAnalyticsCommand}"
                        Style="{StaticResource NavButtonStyle}"/>

                <!-- Spacer -->
                <Border Height="20"/>

                <!-- Info Section -->
                <Border Background="#1A252F" Padding="15" Margin="10">
                    <StackPanel>
                        <TextBlock Text="System Status" FontSize="12" FontWeight="Bold" 
                                   Foreground="White" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Fill="#28A745" Margin="0,0,5,0"/>
                            <TextBlock Text="Backend Connected" FontSize="10" Foreground="#BDC3C7"/>
                        </StackPanel>
                        <TextBlock Text="API: localhost:8080" FontSize="10" 
                                   Foreground="#95A5A6" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Footer -->
                <TextBlock Text="Version 1.0" FontSize="10" Foreground="#7F8C8D" 
                          HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                          Margin="0,20,0,20"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="#F5F5F5">
            <ContentControl Content="{Binding CurrentView}" />
        </Border>
    </Grid>
</Window>

